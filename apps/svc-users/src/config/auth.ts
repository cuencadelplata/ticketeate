// Public endpoints that don't require authentication
export const PUBLIC_ENDPOINTS = [
  '/health',
  '/production/health',
  '/cors-debug',
  '/production/cors-debug',
  '/api/users', // GET /api/users endpoint is public (demo purposes)
];
